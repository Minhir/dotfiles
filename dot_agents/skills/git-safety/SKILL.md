---
name: git-safety
description: Use for any git operation
---

## Readonly commands

Use git only for read-only operations by default.

Do not stage files, create or switch branches, commit, amend, merge, rebase, push, pull, tag, reset, restore, clean, stash, or otherwise change repository state unless the user explicitly asks for that specific operation.
