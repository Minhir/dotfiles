# Dotfiles

## Requirements

- `git`
- `chezmoi`
- `mise`

## Setup

```sh
mise install
```

## Shells

Add the matching line:

```sh
# ~/.bashrc
source ~/.config/bash/rc.bash

# ~/.zshrc
source ~/.config/zsh/rc.zsh
```
